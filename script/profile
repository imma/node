#!/usr/bin/env bash

function profile {
  local shome="${_node_home:-"$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"}"
  PATH="$shome/bin:$shome/exec:$shome/vendor/node/bin:$PATH"
}

profile
